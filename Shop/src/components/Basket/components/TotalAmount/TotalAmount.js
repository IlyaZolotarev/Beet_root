import React from 'react';
import './TotalAmount.scss';

const TotalAmount = ({totalamount}) => {
    return(
    <p className="total-amount">${totalamount}3444</p>
    );
}

export default TotalAmount;